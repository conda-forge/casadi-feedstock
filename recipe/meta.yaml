{% set name = "casadi" %}
{% set version = "3.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ version }}.zip
  url: https://github.com/casadi/casadi/releases/download/{{ version }}/casadi-source-v{{ version }}.zip
  sha256: 4b9c9e9020a1f4e394c472aa49f86da573e3e14170d526d45724d539864cf038
  patches:
    - patches/0001-Rename-qpOASES-namespace-to-casadi_qpOASES.patch
    - patches/0003-Modernize-Python-find_package.patch
    - patches/0004-Bypass-CMake-crosscompiling-conda-forge-CI-issue.patch
    - patches/0005-support-osqp1-backport-pr-4105.patch
    - patches/0006-Fix-FindEigen3-cmake.patch
    # Test for the replacement of the RTLD_DEEPBIND environ workaround, see
    # https://github.com/casadi/casadi/issues/4373
    - patches/0007-Publish-an-owned-environ-snapshot-for-RTLD_DEEPBIND-.patch

build:
  number: 5
  rpaths:
    - lib/
    - lib/{{ name }}/
  run_exports:
    - {{ pin_subpackage('casadi', max_pin='x.x') }}

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - pkg-config
    - cmake
    - ninja
    - swig
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - clang  # [win]
  host:
    - eigen
    - libblas
    - libcblas
    - liblapack
    - python
    - numpy
    - libosqp
    - proxsuite
    # We add eigen-abi-devel to respect the case B in https://github.com/conda-forge/eigen-feedstock/blob/main/recipe/README.md for proxsuite
    - eigen-abi-devel
    - tinyxml2
    - ipopt
    # libblasfeo is included as it is a public dependency of libfatrop
    - libblasfeo  # [unix]
    - libfatrop  # [unix]
    - mumps-seq
  run:
    - python


test:
  imports:
    - casadi
    # Regression test for https://github.com/conda-forge/casadi-feedstock/pull/136#issuecomment-2927518441
    - casadi.tools.structure3
  files:
    - ipopt_nl.py
  commands:
    - python ipopt_nl.py
    # Regression test for https://github.com/conda-forge/casadi-feedstock/issues/93
    - OMP_CANCELLATION=true OMP_PROC_BIND=true python ipopt_nl.py spral  # [linux]
    # Same, but with the OMP_ variables set from Python at runtime rather than
    # exported into the process before start-up: this exercises the setenv path
    # (environ gets reallocated) before the plugin is dlopen'd.
    - env -u OMP_CANCELLATION -u OMP_PROC_BIND python -c "import os; os.environ['OMP_CANCELLATION']='TRUE'; os.environ['OMP_PROC_BIND']='TRUE'; exec(open('ipopt_nl.py').read())" spral  # [linux]
    - test $(pip list | grep casadi | tr -s " " | grep $PKG_VERSION | wc -l) -eq 1  # [unix]
    - cmake-package-check casadi --targets casadi::casadi
  requires:
    - pip
    - cmake-package-check
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}

about:
  home: http://casadi.org
  license: LGPL-3.0-or-later
  license_file: LICENSE.txt
  summary: CasADi is a symbolic framework for numeric optimization

extra:
  recipe-maintainers:
    - agriyakhetarpal
    - traversaro
    - pstjohn
    - pariterre
